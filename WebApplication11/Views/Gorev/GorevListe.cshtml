@using System.Data
@using YKPortal.Models.Dto
@model DataSet
@{
    ViewBag.Title = "Görev Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";

    DataTable dtKullanicilar = ViewBag.Kullanicilar as DataTable;
    string KullaniciID = Request.Cookies["KullaniciID"].Value;

}


<div class="container-fluid p-4 m-0">
    <div class="row">

        <div class="col-lg-12">
            <div class="content d-flex flex-column flex-column-fluid" id="kt_content">
                <!--begin::Post-->
                <div class="post d-flex flex-column-fluid" id="kt_post">
                    <!--begin::Container-->
                    <div id="kt_content_container p-0" class="container-fluid p-0 m-0">
                        <div class="card card-flush">

                            <div class="card-body px-4 scroll-x" align="center">
                                @foreach (DataRow satir in Model.Tables[1].Rows)
                                {
                                    <div style="text-align:center;float:left;padding:5px;">
                                        <div class="symbol symbol-circle symbol-50px overflow-hidden me-3">
                                            <div class="symbol-label">
                                                <img src="@(Convert.ToString(satir["Resim"]))" alt="@(Convert.ToString(satir["Isim"]))" class="w-100 h-100 object-fit-cover" />

                                            </div>
                                        </div>
                                        <br />
                                        <span>@(Convert.ToString(satir["Isim"]))</span>
                                        <br />
                                        <span style="font-size:15px;"><strong>@(Convert.ToString(satir["Miktar"]))</strong></span>
                                    </div>
                                }
                                <div style="clear:both;"></div>
                            </div>
                            <div class="card-header align-items-center py-5 gap-2 gap-md-5 px-4">

                                <div class="card-title">
                                    <!--begin::Search-->
                                    <div class="d-flex align-items-center position-relative my-1">

                                        <form action="@Url.Content("~/Gorev/GorevListe")" method="get">
                                            <table>
                                                <tr>
                                                    <td style="text-align:right;"><label for="GorevTipiID">Durum</label></td>
                                                    <td>
                                                        <select id="Durum" name="Durum">
                                                            <option value="">Lütfen Seçim Yapınız</option>
                                                            <option value="Beklemede" @(ViewBag.Durum == "Beklemede" ? "selected" : "")>Beklemede</option>
                                                            <option value="Kapattınız" @(ViewBag.Durum == "Kapattınız" ? "selected" : "")>Kapattınız</option>
                                                            <option value="Kapatıldı" @(ViewBag.Durum == "Kapatıldı" ? "selected" : "")>Tümü Kullanıcılar Kapattı</option>
                                                        </select>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td style="text-align:right;"><label for="GorevTipiID">Görev tipi</label></td>
                                                    <td>
                                                        <select id="GorevTipiID" name="GorevTipiID">
                                                            <option value="">Lütfen Seçim Yapınız</option>
                                                            @foreach (GrupKoduDto gorevTipi in ViewBag.GorevTipleri)
                                                            {
                                                                <option value="@gorevTipi.ID" @(gorevTipi.ID == ViewBag.GorevTipiID ? "selected" : "")>@gorevTipi.Deger</option>
                                                            }
                                                        </select>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td style="text-align:right;"><label for="GorevTipi">Kayıt Tarihi</label></td>
                                                    <td>
                                                        <input type="date" name="Baslangic" id="Baslangic" value="@(Convert.ToDateTime(ViewBag.Baslangic).ToString("yyyy-MM-dd"))" />
                                                        <input type="date" name="Bitis" id="Bitis" value="@(Convert.ToDateTime(ViewBag.Bitis).ToString("yyyy-MM-dd"))" />
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td style="text-align:right;"> <label for="KayitYapanKullanici">Kaydı Açan</label></td>
                                                    <td>
                                                        <select id="KayitYapanKullanici" name="KayitYapanKullanici">
                                                            <option value="">Lütfen Seçim Yapınız</option>
                                                            @foreach (DataRow drKullanici in dtKullanicilar.Rows)
                                                            {
                                                                <option value="@drKullanici["ID"]" @(Convert.ToString(drKullanici["ID"]) == Convert.ToString(ViewBag.KayitYapanKullanici) ? "selected" : "")>@drKullanici["Ad"] @drKullanici["Soyad"] - @drKullanici["Aciklama1"]</option>
                                                            }
                                                        </select>
                                                        <label for="AtananKullanici">Atanan</label>
                                                        <select id="AtananKullanici" name="AtananKullanici">
                                                            <option value="">Lütfen Seçim Yapınız</option>
                                                            @foreach (DataRow drKullanici in dtKullanicilar.Rows)
                                                            {
                                                                <option value="@drKullanici["ID"]" @(Convert.ToString(drKullanici["ID"]) == Convert.ToString(ViewBag.AtananKullanici) ? "selected" : "")>@drKullanici["Ad"] @drKullanici["Soyad"] - @drKullanici["Aciklama1"]</option>
                                                            }
                                                        </select>
                                                        <input type="submit" name="btnGetir" id="btnGetir" value="Getir" class="btn btn-primary btn-sm" />
                                                    </td>
                                                </tr>
                                            </table>




                                        </form>

                                    </div>
                                    <!--end::Search-->
                                    <!--begin::Export buttons-->
                                    <div id="kt_datatable_example_1_export" class="d-none"></div>
                                    <!--end::Export buttons-->
                                </div>
                                <div class="card-toolbar flex-row-fluid justify-content-end gap-5">
                                    <!--begin::Export dropdown-->
                                    <button type="button" class="btn btn-light-primary btn-sm fw-bold" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end">
                                        <i class="ki-duotone ki-exit-down fs-2"><span class="path1"></span><span class="path2"></span></i>
                                        Dışarı aktar
                                    </button>
                                    <!--begin::Menu-->
                                    <div id="kt_datatable_example_export_menu" class="menu menu-sub menu-sub-dropdown menu-column menu-gray-600 menu-state-bg-light-primary fw-semibold fs-7 w-200px p-0" data-kt-menu="true">
                                        <!--begin::Menu item-->
                                        <div class="menu-item p-0">
                                            <a href="#" class="menu-link px-2 py-0" data-kt-export="copy">
                                                Panoya Kopyala
                                            </a>
                                        </div>
                                        <!--end::Menu item-->
                                        <!--begin::Menu item-->
                                        <div class="menu-item p-0">
                                            <a href="#" class="menu-link px-2 py-0" data-kt-export="excel">
                                                Excel olarak dışarı aktar
                                            </a>
                                        </div>
                                        <!--end::Menu item-->
                                        <!--begin::Menu item-->
                                        <div class="menu-item p-0">
                                            <a href="#" class="menu-link px-2 py-0" data-kt-export="csv">
                                                CSV olarak dışarı aktar
                                            </a>
                                        </div>
                                        <!--end::Menu item-->
                                        <!--begin::Menu item-->
                                        <div class="menu-item p-0">
                                            <a href="#" class="menu-link px-2 py-0" data-kt-export="pdf">
                                                PDF olarak dışarı aktar
                                            </a>
                                        </div>
                                        <!--end::Menu item-->
                                    </div>
                                    <!--end::Menu-->
                                    <!--end::Export dropdown-->
                                    <!--begin::Hide default export buttons-->
                                    <div id="kt_datatable_example_buttons" class="d-none"></div>
                                    <!--end::Hide default export buttons-->
                                    <a href="/Gorev/GorevEkle" class="btn btn-primary btn-sm">
                                        <i class="ki-duotone ki-plus fs-2"></i>Görev Ekle
                                    </a>
                                </div>
                            </div>

                            <div class="card-body px-4 scroll-x">
                                <table class="table align-middle rounded-0 table-row-dashed  fs-6 g-5" id="Gorev-datatable">
                                    <thead>
                                        <!--begin::Table row-->
                                        <tr class="text-start" style="font-size:15px;">
                                            <th class="min-w-125px text-gray-700" style="text-align:center;"></th>
                                            <th class="min-w-125px text-gray-700" style="text-align:center;"></th>
                                            <th class="min-w-125px text-gray-700"><strong>Açıklama</strong></th>
                                            <th class="text-end min-w-100px no-export text-gray-700"><strong>İşlemler</strong></th>
                                        </tr>
                                        <!--end::Table row-->
                                    </thead>
                                    <tbody class="text-gray-600 fw-semibold">
                                        @for (var i = 0; i < Model.Tables[0].Rows.Count; i++)
                                        {
                                            string renk = "";
                                            if (Convert.ToString(Model.Tables[0].Rows[i]["KayitYapanKullanici"]) != KullaniciID)
                                            {
                                                renk = "background-color:#FFE4C4;color:black;";
                                            }
                                            else
                                            {
                                                renk = "color:black;";
                                            }
                                            <tr style="@(renk)">
                                                <td style="padding-left:3px;width:150px;vertical-align:top;text-align:center;">
                                                    <table>
                                                        <tr>
                                                            <td style="text-align:center;">
                                                                <div class="symbol symbol-circle symbol-50px overflow-hidden me-3">
                                                                    <div class="symbol-label">
                                                                        <img src="@(Convert.ToString(Model.Tables[0].Rows[i]["Resim"]))" alt="@(Convert.ToString(Model.Tables[0].Rows[i]["KaydiAcan"]))" class="w-100 h-100 object-fit-cover" />
                                                                    </div>
                                                                </div>
                                                                <br />
                                                                @(Convert.ToString(Model.Tables[0].Rows[i]["KaydiAcan"]))

                                                            </td>
                                                            <td>
                                                              
                                                            </td>
                                                        </tr>
                                                        <tr>
                                                            <td colspan="2">

                                                                <span>
                                                                    @{
                                                                        foreach (string item in Convert.ToString(Model.Tables[0].Rows[i]["AtananKullanicilar"]).Split('|'))
                                                                        {
                                                                            if (item.Trim().Length > 0)
                                                                            {
                                                                                <div style="float:left;text-align:center;margin-right:3px;">
                                                                                    <div class="symbol symbol-circle symbol-20px overflow-hidden me-3">
                                                                                        <div class="symbol-label">
                                                                                            <img src="@(item.Split('~')[1])" alt="@(item.Split('~')[0])" class="w-100 h-100 object-fit-cover" />
                                                                                        </div>
                                                                                    </div><br />
                                                                                    <span style="font-size:8px;margin:0px;padding:0px;">@(item.Split('~')[0])</span>
                                                                                </div>
                                                                            }
                                                                        }

                                                                    }
                                                                </span>
                                                            </td>
                                                        </tr>
                                                    </table>
                                                </td>
                                                <td style="vertical-align: top; width: 150px; ">
                                                    <strong>@(Convert.ToDateTime(Model.Tables[0].Rows[i]["BaslangicTarihi"]).ToString("dd-MM-yyyy HH:mm"))</strong>
                                                    <br />
                                                    @if (Convert.ToString(Model.Tables[0].Rows[i]["Periyot"]) == "T")
                                                    {
                                                        <span style="color: green;">Tek Seferlik</span>
                                                    }
                                                    else if (Convert.ToString(Model.Tables[0].Rows[i]["Periyot"]) == "G")
                                                    {
                                                        <span style="color: blue;">Günlük</span>
                                                    }
                                                    else if (Convert.ToString(Model.Tables[0].Rows[i]["Periyot"]) == "H")
                                                    {
                                                        <span style="color: orange;">Haftalık</span>
                                                    }
                                                    else if (Convert.ToString(Model.Tables[0].Rows[i]["Periyot"]) == "Y")
                                                    {
                                                        <span style="color: red;">Yıllık</span>
                                                    }
                                                    else
                                                    {
                                                        <span>Bilinmeyen</span>
                                                    }
                                                    <span style="color:green;">@Convert.ToString(Model.Tables[0].Rows[i]["Durum"])</span>
                                                    <span>@(Convert.ToString(Model.Tables[0].Rows[i]["GorevAtamaSayisi"]))/@(Convert.ToString(Model.Tables[0].Rows[i]["GorevHareketSayisi"]))</span>

                                                </td>
                                                <td style="vertical-align: top;">
                                                    <span style="color:orange;">@(Convert.ToString(Model.Tables[0].Rows[i]["Gorev"]))</span>
                                                    <br />
                                                    @(Convert.ToString(Model.Tables[0].Rows[i]["Aciklama"]))
                                                </td>

                                                <td class="text-end" style="padding-right: 3px; vertical-align: top;width:150px;">

                                                    @if (Convert.ToString(Model.Tables[0].Rows[i]["KayitYapanKullanici"]) == KullaniciID)
                                                    {
                                                        <a href="#" class="btn btn-light btn-active-light-primary btn-flex btn-center btn-sm" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end">
                                                            İşlemler
                                                            <i class="ki-duotone ki-down fs-5 ms-1"></i>
                                                        </a>
                                                        <!--begin::Menu-->
                                                        <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg-light-primary fw-semibold fs-7 w-125px p-0" data-kt-menu="true">
                                                            <!--begin::Menu item-->
                                                            <div class="menu-item p-0">
                                                                <a href="/Gorev/GorevDuzenle/@(Convert.ToString(Model.Tables[0].Rows[i]["ID"]))" class="menu-link py-0  px-2">Detay / Düzenle</a>
                                                            </div>
                                                            <!--end::Menu item-->
                                                            <!--begin::Menu item-->
                                                            <div class="menu-item p-0">
                                                                <button type="button" data-id="@(Convert.ToString(Model.Tables[0].Rows[i]["ID"]))" class="btn btn-sm w-100 menu-link rounded-0 px-2 py-0 btn-sil" data-bs-toggle="modal" data-bs-target="#Gorev-silme">
                                                                    Sil
                                                                </button>

                                                            </div>
                                                            <!--end::Menu item-->
                                                        </div>
                                                        <!--end::Menu-->
                                                    }
                                                    else
                                                    {
                                                        <a href="/Gorev/GorevDuzenle/@(Convert.ToString(Model.Tables[0].Rows[i]["ID"]))" class="btn btn-light btn-active-light-primary btn-flex btn-center btn-sm">Detay'a Git</a>

                                                    }
                                                    <br />
                                                    <span style="color:gray;">@Convert.ToDateTime(Model.Tables[0].Rows[i]["KayitTarihi"]).ToString("dd-MM-yyyy HH:mm")</span>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <!--end::Container-->
                </div>
                <!--end::Post-->
            </div>

        </div>
    </div>
</div>

<div class="modal fade" id="Gorev-silme" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Görev Silme İşlemi</h3>

                <!--begin::Close-->
                <div class="btn btn-icon btn-sm btn-active-light-primary ms-2" data-bs-dismiss="modal" aria-label="Close">
                    <i class="ki-duotone ki-cross fs-1"><span class="path1"></span><span class="path2"></span></i>
                </div>
                <!--end::Close-->
            </div>

            <div class="modal-body">
                <p>Görevi silmek istediğinize emin misiniz?</p>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-light btn-sm fw-bold" data-bs-dismiss="modal">Vazgeç</button>
                <form method="post" id="silme-formu" action="/Gorev/GorevSil">
                    <input type="hidden" name="id" value="" />

                    <button type="submit" class="btn btn-danger btn-sm fw-bold">Sil</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            document.querySelectorAll('.btn-sil').forEach(silButonu => {
                silButonu.addEventListener('click', function () {
                    document.querySelector('form#silme-formu input[name="id"]').value = silButonu.dataset.id;
                })
            });


        });

        // Class definition
        var KTDatatablesExample = function () {
            // Shared variables
            var table;
            var datatable;

            // Private functions
            var initDatatable = function () {
                // Set date data order
                const tableRows = table.querySelectorAll('tbody tr');

                // Init datatable --- more info on datatables: https://datatables.net/manual/
                datatable = $(table).DataTable({
                    "info": false,

                    'order': [],
                    paging: false,
                    columnDefs: [
                        {
                            target: [],
                            visible: false
                        },
                        {
                            target: [],
                            visible: false
                        }
                    ]

                });

                datatable.on('columns-reordered', function (e, settings, details) {
                    KTMenu.createInstances();
                });
            }

            // Hook export buttons
            var exportButtons = () => {
                const documentTitle = 'Gorev-Listesi-' + moment(new Date()).format("DD-MM-YYYY");
                var buttons = new $.fn.dataTable.Buttons(table, {
                    buttons: [
                        {
                            extend: 'copyHtml5',
                            title: documentTitle,
                            exportOptions: {
                                columns: ['th:not(.no-export)'],
                            }
                        },
                        {
                            extend: 'excelHtml5',
                            title: documentTitle,
                            exportOptions: {
                                columns: ['th:not(.no-export)'],
                            }
                        },
                        {
                            extend: 'csvHtml5',
                            title: documentTitle,
                            exportOptions: {
                                columns: ['th:not(.no-export)'],
                            }
                        },
                        {
                            extend: 'pdfHtml5',
                            title: documentTitle,
                            exportOptions: {
                                columns: ['th:not(.no-export)'],
                            }
                        }
                    ]
                }).container().appendTo($('#kt_datatable_example_buttons'));

                // Hook dropdown menu click event to datatable export buttons
                const exportButtons = document.querySelectorAll('#kt_datatable_example_export_menu [data-kt-export]');
                exportButtons.forEach(exportButton => {
                    exportButton.addEventListener('click', e => {
                        e.preventDefault();

                        // Get clicked export value
                        const exportValue = e.target.getAttribute('data-kt-export');
                        const target = document.querySelector('.dt-buttons .buttons-' + exportValue);

                        // Trigger click event on hidden datatable export buttons
                        target.click();
                    });
                });
            }

            // Search Datatable --- official docs reference: https://datatables.net/reference/api/search()
            var handleSearchDatatable = () => {
                const filterSearch = document.querySelector('[data-kt-filter="search"]');
                filterSearch.addEventListener('keyup', function (e) {
                    datatable.search(e.target.value).draw();
                });
            }

            // Public methods
            return {
                init: function () {
                    table = document.querySelector('#Gorev-datatable');

                    if (!table) {
                        return;
                    }

                    initDatatable();
                    exportButtons();
                    handleSearchDatatable();
                }
            };
        }();

        // On document ready
        KTUtil.onDOMContentLoaded(function () {
            KTDatatablesExample.init();
        });
    </script>
}


